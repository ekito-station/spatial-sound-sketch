using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PinController : ObjectController
{
    // Start is called before the first frame update
    void Start()
    {
        isSound = false;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
